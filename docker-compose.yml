
version: '3.9'

services:
  mariadb:
    build: ./srcs/MariaDB
    container_name: mariadb
    ports:
      - "8000:8000"

  nginx:
    build: ./srcs/NGINX
    container_name: nginx
    ports:
      - "443:443"

  php:
    build: ./srcs/PHP
    container_name: php
    ports:
      - "3000:3000"

